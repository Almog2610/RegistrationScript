class HasteUser:

    def __int__(self, first_name, email, username, password):
        self.first_name = first_name
        self.email = email
        self.username = username
        self.password = password
